LiteQMS — Session Log
Date: 24 May 2026
Project: D:\Programs\liteqms
Model: opencode/deepseek-v4-flash-free

============================================================
SESSION SUMMARY
============================================================

1. Completed Stage 5 — Production Readiness (Items 5b-5f)

2. Implemented Network & LAN Access features

3. Port auto-fallback feature

============================================================
ITEM 5b — Auto-open browser on startup
============================================================

Replaced app.Run() with StartAsync() + WaitForShutdownAsync().
Browser auto-opens to server URL in production (try-catch wrapped).
Production DB path resolves to %APPDATA%\LiteQMS\ (safe from Program Files).

============================================================
ITEM 5f — publish.bat
============================================================

Created one-liner: dotnet publish -c Release -r win-x64 --self-contained true -o .\dist

============================================================
ITEM 5e — Inno Setup installer
============================================================

Created setup.iss for Inno Setup 7.
Installs to {autopf}\LiteQMS. DB stored in %APPDATA%\LiteQMS\ (preserved on reinstall/uninstall).
Desktop + Start Menu shortcuts. Launch after install option.
Uninstall removes app files, leaves DB.

============================================================
ITEM 5c — System tray icon
============================================================

Program.cs restructured to [STAThread] Main for WinForms.
NotifyIcon with "Open LiteQMS" / "Quit" right-click menu.
Double-click opens browser. Quit → graceful server shutdown.
Falls back silently if tray unavailable (no GUI).

============================================================
ITEM 5d — In-app update checker
============================================================

Pages/Updates.cshtml + Updates.cshtml.cs created.
Fetches latest release from api.github.com/repos/spd3ictpro/liteqms.
1-hour cache via IMemoryCache. Three states: up-to-date, update available, check failed.
"Check for updates" link in shared _Layout.cshtml footer.
Assembly version: 1.0.0 from .csproj.

============================================================
NETWORK & LAN ACCESS
============================================================

Urls config set to http://0.0.0.0:5000 (all network interfaces).
Hostname URL (http://PC-NAME:5000) shown in:
  - Index page banner at card bottom
  - Tray icon tooltip on hover
  - %APPDATA%\LiteQMS\startup.log (persistent log file)

Auto-open uses localhost:5000 for the server PC user.
setup.iss updated with:
  - PowerShell command to switch Wi-Fi from Public to Private during install
  - Netsh command to add Windows Firewall rule for port 5000
  - Netsh command to remove rule on uninstall

============================================================
UI IMPROVEMENTS
============================================================

OutputType changed to WinExe (no console window).
Removed UseHttpsRedirection (eliminates confusing HTTPS warning on LAN).
Custom liteqms.ico applied to tray icon, .exe, installer, and shortcuts.
"Not Secure" warning accepted as cosmetic — users tap Proceed once.

============================================================
PORT AUTO-FALLBACK
============================================================

Program.cs: FindFreePort() method checks active TCP listeners.
If port 5000 taken, increments until a free port is found.
All URLs (tray, Index page, log) dynamically reflect actual port.
Index.cshtml.cs reads port from IConfiguration (LiteQMS:Port).
Dev mode skips fallback — uses configured port as-is.

============================================================
FILES CHANGED THIS SESSION
============================================================

Modified (9):
  .gitignore
  DEVELOPMENT_SUMMARY.md
  LiteQMS.csproj
  Pages/Index.cshtml
  Pages/Index.cshtml.cs
  Pages/Shared/_Layout.cshtml
  Program.cs
  appsettings.json
  setup.iss

New (6):
  liteqms.ico
  Pages/Updates.cshtml
  Pages/Updates.cshtml.cs
  publish.bat
  setup.iss
  session-24-may-2026.txt

============================================================
HOW TO RUN
============================================================

Development:
  dotnet run --urls "http://localhost:5000"

Production:
  Run .\dist\LiteQMS-Setup.exe to install
  Or: .\dist\LiteQMS.exe --urls "http://localhost:5000"

Pages:
  Room setup:    http://localhost:5000/
  Call panel:    http://localhost:5000/CallPanel
  TV display:    http://localhost:5000/Display
  History:       http://localhost:5000/History
  Updates:       http://localhost:5000/Updates

Other devices: http://PC-NAME:5000/{page}
